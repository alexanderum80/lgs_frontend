query AuthenticateUser($user: String!, $passw: String!) {
  authenticateUser(user: $user, passw: $passw) {
    Id
    UserName
    Name
    LastName
    Enabled
    Token
    UserRoles {
      IdRole
    }
  }
}